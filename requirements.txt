Django>=4.2,<5.0
Pillow>=10.0.0
python-decouple>=3.8
mysqlclient>=2.2.0
channels>=4.0.0
daphne>=4.0.0
channels-redis>=4.1.0
django-crispy-forms>=2.0,<3.0
django-filter>=24.0,<25.0
staticmap>=0.5.7
django-axes>=6.0,<7.0
# Caching: Redis backend for production (LocMemCache used as fallback)
django-redis>=5.4,<6.0
# Static file serving with hashed filenames, compression, and cache headers
whitenoise>=6.6,<7.0
# Transactional email via Anymail (Elastic Email backend)
django-anymail>=10.1
# Stripe payment gateway
stripe>=15.0
